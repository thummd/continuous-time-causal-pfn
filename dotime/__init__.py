"""Do-Over-Time-PFN: In-context causal effect estimation for temporal data.

The temporal-SCM prior (``causal_time_prior``) and the minimal ``dopfnprior``
graph/mechanism utilities it needs are vendored at the repository root, so no
external repositories or ``PYTHONPATH`` manipulation are required.
"""
